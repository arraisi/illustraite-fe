# Suggested Commands

## Development
```bash
npm install          # Install dependencies
npm run dev          # Start dev server (Vite)
npm run build        # Production build
npm run preview      # Preview production build
```

## No linting/testing/formatting configured
No test framework, linter, or formatter is set up in package.json.

## Environment
Required `.env` variables:
```
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_WS_BASE_URL=ws://localhost:8080/ws/v1
```

## System Utils (macOS/Darwin)
```bash
git, ls, cd, grep, find  # Standard unix utils available
```
